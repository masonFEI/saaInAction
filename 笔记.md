# 1 Spring AI Alibaba之理论概述

学习网址：https://java2ai.com/

# 2 永远的HelloWorld

阿里百炼平台的通义模型

spring-boot开发5步骤

1. 建module
2. 改pom
3. 写YML
4. 主启动
5. 业务类

# 3 Ollama私有化部署和对接本地大模型

docker Hub玩镜像;Ollama Hub玩模型

OllamaSetup.exe /DIR=E:\MySoftware\Ollama

环境变量：

(存放大模型数据包的目录)
OLLAMA_MODELS:XXXX

(查看OLLAMA是否安装成功)
netstat -ano | findstr 11434

ollama run qwen3:4b

# 4 ChatClient VS ChatModel

## ChatModel

它是底层接口，直接与具体大语言模型交互，提供`call()` 和`stream()`方法，适合简单大模型交互场景。

## ChatClient

它提供了与AI模型通信的FluentAPI,它支持同步和反应式（Reactive）编程模型。

当然使用ChatModel等原子API可以为应用程序带来更多的灵活性，成本就是需要编写大量样板代码。

ChatClient类似于应用程序开发中的服务层，它为应用程序直接提供AI服务，开发者可以使用ChatClient Fluent API 快速完成一整套AI交互流程的组装。

包括一些基础功能，如：

- 定制和组装模型的输入（Prompt）
- 格式化解析模型的输出（Structured Output）
- 调整模型交互参数（ChatOptions）

还支持更多高级功能：

- 聊天记忆（Chat Memory）
- 工具/函数调用（Function Calling）
- RAG

# 5 Server-SentEvents (SSE)实现Stream流式输出及多模型共存

## 流式输出

是一种逐步返回大模型生成结果的技术，允许服务器将响应内容分批次实时传输给客户端。
通过单向的HTTP长连接，让服务器可以主动将数据“推”给客户端，SSE是轻量级的单向通信协议。
适合AI对话这类服务端主导的场景。

## Server-SentEvents（SSE）与WebSocket的区别

| 特性       | SSE                   | WebSocket                |
|------------|-----------------------|--------------------------|
| 通信方向   | 单向（服务器→客户端） | 双向（全双工）           |
| 协议       | HTTP                  | 独立的WS协议             |
| 数据类型   | 文本                  | 文本和二进制             |
| 自动重连   | 是                    | 需要手动实现             |
| 实现复杂度 | 简单，基于HTTP        | 较复杂，需要握手升级     |
| 适用场景   | AI流式输出、实时通知  | 实时聊天、协作编辑、游戏 |

SSE下一代Streamable Http, 通信方向是双向的

## springBoot3响应式编程
